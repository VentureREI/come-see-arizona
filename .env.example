# Copy to .env.local and fill in. .env.local is gitignored.
# Perplexity API key (https://www.perplexity.ai/settings/api) — used ONLY by
# scripts/market-refresh.ts at build/refresh time. Never expose with a VITE_ prefix.
PERPLEXITY_API_KEY=
